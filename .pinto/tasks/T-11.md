+++
id = "T-11"
title = "Scaffold OpenCode /avril and /axel agents + commands"
status = "todo"
rank = "s"
points = 5
labels = ["opencode", "avril-blessed"]
created = "2026-08-12T05:26:38.943791Z"
updated = "2026-08-12T05:26:39.014137Z"
+++

## why
Users need first-class slash entrypoints so AVRIL/AXEL are one keystroke away instead of buried in free chat.

## scope_in
- Create project-scoped OpenCode layout:
  - `.opencode/opencode.jsonc` with `$schema` (minimal; preserve any existing if present)
  - `.opencode/agent/avril.md` — mode: primary; thin prompt → load skill `avril` + `code-writer`; planning-only; color distinct
  - `.opencode/agent/axel.md` — mode: primary; thin prompt → load `axel` + language stack; intake gate; conductor-only; color distinct
  - `.opencode/command/avril.md` — description; agent: avril; body uses `$ARGUMENTS`; empty args = status
  - `.opencode/command/axel.md` — description; agent: axel; empty args = next ready PBI + **confirm before execute**
- Agents: permission.edit ask (or deny write for avril where practical); bash allow read-only git/pinto patterns, ask on writes
- Do not duplicate full SKILL.md text — activate skills via skill tool / instructions
- Document in command body: restart opencode after install
- Smoke: files valid markdown frontmatter; paths match OpenCode discovery (`command/` + `agent/`)

## scope_out
- Full preflight shell injections (cmd follow-on)
- harness-bootstrap / templates (later)
- Rewriting `.agents/skills/avril|axel`
- Global `~/.config/opencode` install
- Separate micro-commands per verb

## acceptance_criteria
- [ ] `.opencode/command/avril.md` and `axel.md` exist with `$ARGUMENTS` and agent frontmatter
- [ ] `.opencode/agent/avril.md` and `axel.md` exist with mode primary and thin conductor prompts
- [ ] Empty `/avril` path documented as read-only status
- [ ] Empty `/axel` path documented as next-PBI summary + confirm (no auto-run)
- [ ] AVRIL agent/command text forbids implementing production code
- [ ] AXEL agent/command text enforces intake gate
- [ ] `just harness-validate` still passes

## notes
Decisions: primary mode; project-scoped; axel empty = confirm. Plan: docs/plans/opencode-avril-axel-commands.html
