+++
id = "T-1"
title = "Ship public skills allowlist SSOT + README catalog"
status = "done"
rank = "i"
points = 3
labels = ["docs", "avril-blessed"]
start_at = "2026-08-12T03:20:25.718350Z"
done_at = "2026-08-12T03:24:23.132997Z"
created = "2026-08-05T01:19:07.029287Z"
updated = "2026-08-12T03:24:23.132997Z"
+++

## why
Adopters and CI need one machine-readable list of skills we claim publicly so counts and tables cannot drift.

## scope_in
- Add `docs/public-skills.json` with stable schema:
  - `version` (number)
  - `skills`: array of `{ "name", "category", "purpose" }`
  - `out`: array of `{ "name", "reason" }` for non-public skills/stubs
- v1 `skills` names exactly: code-writer, rust-code-writer, rust-code-reviewer, rust-code-tester, rust-architect, rust-team-lead, avril, axel, rust-axum-backend, rust-frontend, rust-tui, rust-errors, agent-harness, skill-evaluator (N=14)
- `out` includes at least: obsidian-cli, diataxis (deferred), diataxis-explanation, diataxis-how-to-guides, diataxis-reference
- Sync root README skills table to `skills[].name` exactly (add skill-evaluator; no extras)
- No marketing redesign; no book pipeline chapters in this PBI

## scope_out
- Per-skill book pages
- site/templates/index.html rewrite
- docs-verify automation (later PBI)
- Adding diataxis to public `skills` array

## acceptance_criteria
- [x] `docs/public-skills.json` validates against the documented schema (version + skills[] + out[])
- [x] `skills[].name` is exactly the 14 allowlisted names (set equality)
- [x] Every `skills[].name` has non-empty `.agents/skills/<name>/SKILL.md`
- [x] README skills table names == `skills[].name` set (no missing, no extras)
- [x] `out` documents obsidian-cli, diataxis deferred, and empty diataxis-* stubs with reasons
- [x] `just harness-validate` still passes

## notes
none

## github
- https://github.com/scull7/crossr-skills/issues/48
