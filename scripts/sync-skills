#!/usr/bin/env bash
# sync-skills — removed in split-07.
# Canonical installer: sycamore-hq/crossr-harness (pins skills = <tag>, loops = <tag>).
echo "deprecated: use sycamore-hq/crossr-harness" >&2
exit 1
